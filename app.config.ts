export default defineAppConfig({
  ui: {
    primary: 'brand',
    icons: ["ph"],
    modal: {
      container: 'items-center'
    },
    card: {
        body: {
            base: 'flex flex-col h-full'
        }
    }
  }
})