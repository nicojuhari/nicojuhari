export default defineAppConfig({
  ui: {
    primary: 'brand',
    modal: {
      container: 'items-center'
    },
    card: {
        body: {
            base: 'flex flex-col h-full'
        }
    }
  }
})