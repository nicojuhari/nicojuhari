export default defineAppConfig({
  ui: {
    icons: ["ph"],
    modal: {
      container: 'items-center'
    },
    card: {
        body: {
            base: 'flex flex-col h-full'
        }
    },
  }
})